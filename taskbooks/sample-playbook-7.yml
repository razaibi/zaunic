---
nodes: test-nodes
secrets:
  test-secret:
    keyname: "client_id"
    env: "dev"
    source: "zaunic_local"
tasks:
  - name: "Impute Secret"
    action: "generate"
    category: "bash"
    template: "print>secret"
    output: "/>something>secrets_demo.sh"
    